{
  "compilerOptions": {
    "target": "ES6",              // Версия JavaScript, в которую вы хотите компилировать
    "module": "commonjs",         // Модульная система
    "outDir": "./dist",           // Папка, куда будут сохраняться скомпилированные файлы
    "rootDir": "./src",           // Папка с исходными TypeScript файлами
    "strict": true,               // Включение строгой проверки типов
    "esModuleInterop": true,      // Для совместимости с ES модулями
    "skipLibCheck": true          // Пропустить проверку библиотек
  },
  "include": ["src/**/*"],        // Укажите папку, из которой компилировать файлы
  "exclude": ["node_modules"]     // Папка, которую нужно исключить из компиляции
}
